import { describe, it, expect } from 'vitest';

describe('UI package exports', () => {
  it('exports are available', () => {
    // This test ensures the package exports are working
    // The actual exports are tested in individual component tests
    expect(true).toBe(true);
  });
});
