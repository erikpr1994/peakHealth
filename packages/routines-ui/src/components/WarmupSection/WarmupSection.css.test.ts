import { describe, it, expect } from 'vitest';
import './WarmupSection.css';

describe('WarmupSection CSS', () => {
  it('should import CSS file without errors', () => {
    expect(true).toBe(true);
  });
});
