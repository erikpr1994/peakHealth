{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Next.js Configuration",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Next.js specific settings
    "plugins": [{ "name": "next" }],

    // Path aliases - will be overridden by app-specific configs if needed
    "paths": { "@/*": ["./src/*"] }
  }
}
